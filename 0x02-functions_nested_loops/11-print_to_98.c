#include "main.h"
#include <stdio.h>

/**
 * add - adds to integers
 * @n: integer param
 * Return: 0 if exited properly, non-zero otherwise
 */

void print_to_98(int n)
{
int i;

for (i=n; i <= 98; i++)
{
printf('%d, ', i);
}
}
